feature:
  name: property
  description: Property-based testing with generators and shrinking
  status: implemented
  confidence: 0.95

triangulation:
  docs_claim: "Property-based testing with shrinking"
  code_reality: "Full implementation with generators, shrinking, runner, and reporting"
  test_coverage: high
  classification: convergent

capabilities:
  - Generate random test data
  - Verify properties across many test cases
  - Automatically shrink failing cases to minimal examples
  - Integrate with FiveAM test framework
  - Extensible generator system

dependencies:
  system: cl-test-hardening/property
  depends_on:
    - cl-test-hardening (core)
  provides:
    - property-based testing
    - generator combinators
    - shrinking algorithms

packages:
  - th.property

contracts:
  - generators.md
  - properties.md

scenarios:
  - property-checking.md
  - generator-usage.md

properties:
  - generator-invariants.md
  - property-testing-guarantees.md

files:
  implementation:
    - src/property/package.lisp
    - src/property/random.lisp
    - src/property/core.lisp
    - src/property/shrinking.lisp
    - src/property/runner.lisp
    - src/property/reporting.lisp
    - src/property/macros.lisp
  generators:
    - generators/primitives.lisp
    - generators/collections.lisp
    - generators/combinators.lisp
  tests:
    - tests/generator-tests.lisp
    - tests/shrinking-tests.lisp
    - tests/property-tests.lisp
