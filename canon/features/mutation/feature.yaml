feature:
  name: mutation
  description: Mutation testing for test suite quality verification
  status: implemented
  confidence: 0.95

triangulation:
  docs_claim: "Mutation testing for test quality verification"
  code_reality: "Full implementation with operators, mutator, runner, and reporting"
  test_coverage: high
  classification: convergent

capabilities:
  - Apply mutation operators to code
  - Run tests against mutated code
  - Track killed vs survived mutants
  - Report mutation score
  - Extensible operator system

dependencies:
  system: cl-test-hardening/mutation
  depends_on:
    - cl-test-hardening (core)
    - cl-ppcre
  provides:
    - mutation testing
    - standard operators
    - mutation analysis

packages:
  - th.mutation

contracts:
  - mutation-operators.md
  - mutation-testing.md

scenarios:
  - mutation-analysis.md

properties:
  - mutation-correctness.md

files:
  implementation:
    - src/mutation/package.lisp
    - src/mutation/core.lisp
    - src/mutation/pattern-matching.lisp
    - src/mutation/mutator.lisp
    - src/mutation/runner.lisp
    - src/mutation/reporting.lisp
    - src/mutation/macros.lisp
  operators:
    - operators/standard.lisp
  tests:
    - tests/mutation-tests.lisp
